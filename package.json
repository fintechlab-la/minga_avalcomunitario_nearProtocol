{
  "name": "singleton-counter",
  "version": "1.0.0",
  "description": "simple example of a single counter in SA",
  "main": "index.js",
  "scripts": {
    "test": "asp",
    "build": "asb",
    "deploy:dev": "yarn build && near dev-deploy"
  },
  "author": "FTL",
  "license": "MIT",
  "devDependencies": {
    "near-sdk-as": "^3.2.3"
  }
}
